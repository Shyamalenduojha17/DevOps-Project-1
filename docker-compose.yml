version: "3"
services:
  nginx:
    image: nginx:stable-alpine3.17
    ports:
      - "81:80"
    volumes:
      - ./Nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - node-app
    restart: always
  node-app:
    build: .
    ports:
      - "3000:3000"
